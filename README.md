# Employee Management System

## 📌 Project Overview
The **Employee Management System** is a dynamic web application developed using Java, Servlets, JSP, and JDBC. It follows the **MVC (Model-View-Controller)** architecture to provide a streamlined interface for managing employee data. The system allows administrators to perform CRUD (Create, Read, Update, Delete) operations on employee records and includes a secure login module.

## 🚀 Features
* **User Authentication:** Secure login page to prevent unauthorized access.
* **Add New Employee:** Form to register new employees with details like Name, Email, Country, and Role.
* **View All Employees:** Displays a list of all employees stored in the database.
* **Update Employee:** Edit existing employee details.
* **Delete Employee:** Remove employee records from the system.
* **MVC Architecture:** Clean separation of concerns between Data (Model), UI (View), and Logic (Controller).

## 🛠️ Technology Stack
* **Language:** Java (JDK 8+)
* **Server-Side:** Java Servlets, JSP (JavaServer Pages)
* **Database:** MySQL
* **Data Access:** JDBC (Java Database Connectivity)
* **Server:** Apache Tomcat 9
* **Frontend:** HTML, CSS, JSP
* **Build Tool:** Maven / Gradle (optional, or manual classpath management)

## 📂 Project Structure
The project follows a standard directory structure:



